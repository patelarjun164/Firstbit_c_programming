#include<stdio.h>
#include<string.h>

int main(){
    // char str[50];
    // scanf("%s", str);

    // char str2[30];
    // scanf("%s", str2);

    // printf("%u \n", strstr(str, str2)-str);
    return 0;
}